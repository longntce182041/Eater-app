from typing import Optional

# Placeholder for security-related utilities, e.g.:
# - parsing JWT claims coming from backend
# - validating request signatures if needed

def get_user_id_from_token(token: str) -> Optional[str]:
    # TODO: parse/validate JWT from upstream backend if necessary
    return None