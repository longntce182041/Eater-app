import 'package:flutter/material.dart';

class MealLoggingPage extends StatelessWidget {
  const MealLoggingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Meal Logging'),
      ),
      body: const Center(
        child: Text('Meal Logging Page - To be implemented'),
      ),
    );
  }
}
