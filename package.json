{
  "name": "eater-app",
  "version": "1.0.0",
  "description": "AI Healthy Meal Planner - Multi-service application",
  "private": true,
  "scripts": {
    "test:connection": "node test-ai-connection.js",
    "start:ai": "cd ai_meal_planing_service && uvicorn app.main:app --reload --port 8000",
    "start:backend": "cd eater-backend && npm run dev",
    "start:frontend": "cd eater-front-end && npm run dev"
  },
  "dependencies": {
    "axios": "^1.13.2"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=16.0.0"
  }
}
